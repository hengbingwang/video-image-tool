# PRD：视频图片处理工具

**版本**：v1.0
**更新日期**：2026-03-08
**文档状态**：评审中
**适用对象**：研发、测试

---

## 目录

1. [产品概述](#1-产品概述)
2. [用户角色与典型使用场景](#2-用户角色与典型使用场景)
3. [功能模块详细需求](#3-功能模块详细需求)
   - 3.1 视频截图提取
   - 3.2 图片拼接
   - 3.3 AI 调研报告生成
   - 3.4 跨模块工作流（执行按钮）
4. [非功能性需求](#4-非功能性需求)
5. [技术约束与部署说明](#5-技术约束与部署说明)
6. [未来规划](#6-未来规划)
7. [验收标准](#7-验收标准)

---

## 1. 产品概述

### 1.1 背景

产品/设计/研究人员在做竞品分析、产品评审、用户研究时，常需要从视频录屏中提取关键帧、将截图整理成拼图、再用 AI 生成结构化报告。目前这三个步骤依赖不同工具且需手动衔接，效率低。

### 1.2 产品定位

一个集成「视频截图提取 → 图片拼接 → AI 报告生成」三段式工作流的 Web 工具，面向内部研究/设计/产品人员，支持多用户独立使用（无账号体系，无协同需求）。

### 1.3 核心价值

| 痛点 | 解决方案 |
|------|----------|
| 手动截视频帧费时 | 场景自动检测 + 手动补帧，一键批量截图 |
| 截图散乱、难排版 | 可视化拖拽排序 + 灵活网格拼接导出 |
| 报告撰写耗时 | 图片直接喂给 Claude，按模板生成结构化分析 |
| 三步工具切换麻烦 | 单页 Tab 工具，截图可一键发送到拼接或报告 |

### 1.4 技术栈概览

- **后端**：Python + FastAPI
- **前端**：原生 HTML / CSS / JavaScript（无框架）
- **AI**：Anthropic Claude API（claude-sonnet-4-6）
- **视频处理**：OpenCV + PySceneDetect
- **图片处理**：Pillow

---

## 2. 用户角色与典型使用场景

### 2.1 用户角色

| 角色 | 描述 | 主要使用功能 |
|------|------|-------------|
| 产品/设计研究员 | 做竞品分析、产品评审 | 全流程：截图 → 报告 |
| 视觉设计师 | 整理素材、制作对比图 | 截图 → 拼接 |
| 产品经理 | 快速产出分析文档 | 截图 → 报告 |

### 2.2 典型使用场景

**场景 A：竞品分析**
1. 上传竞品 App 录屏视频
2. 自动检测场景变化，批量提取关键帧
3. 选中截图，一键发送到「AI 报告生成」
4. 选择「竞品横向对比」模板，生成 Markdown 报告
5. 复制或下载报告

**场景 B：UI 走查**
1. 上传 UI 录屏视频
2. 手动逐帧截取关注节点
3. 发送截图到「图片拼接」，按功能区域排列
4. 导出拼图用于设计评审

**场景 C：研究素材整理**
1. 直接上传已有截图到「图片拼接」
2. 拖拽调整顺序，设置布局参数
3. 同时勾选「图片拼接 + 生成报告」一键执行

---

## 3. 功能模块详细需求

---

### 3.1 视频截图提取

#### 3.1.1 视频上传

| 需求项 | 描述 |
|--------|------|
| 支持方式 | 点击选择文件 或 拖拽文件到上传区 |
| 支持格式 | 主流视频格式（MP4、MOV、AVI 等，依赖 OpenCV 解码能力） |
| 上传后 | 自动在页面内嵌播放器中加载该视频 |
| 会话隔离 | 每次上传创建独立 `video_id`，数据存储在 `temp/{video_id}/` 目录下 |

#### 3.1.2 视频播放器

| 需求项 | 描述 |
|--------|------|
| 播放控制 | 原生 HTML5 播放器，支持播放/暂停/拖动进度 |
| 时间戳显示 | 实时显示当前帧时间，格式 `HH:MM:SS.ms`（精确到百毫秒） |
| 手动截帧按钮 | 点击「📷 截取此帧」，截取播放器当前帧 |

#### 3.1.3 场景自动检测

**算法选择**（三选一）：

| 算法 | 适用场景 | 阈值范围 | 默认值 |
|------|----------|----------|--------|
| Content（内容检测） | 通用，适合大多数视频 | 1 – 100 | 27 |
| Adaptive（自适应） | 光线变化频繁的视频 | 1 – 10 | 3 |
| Threshold（阈值） | 纯色背景切换 | 1 – 255 | 12 |

**参数配置**：
- 阈值滑块：滑动时气泡实时显示当前值
- 切换算法时，阈值范围与默认值自动重置
- 最小场景时长（`min_scene_len`）：0.1s – 60s，用于过滤短镜头

**执行**：
- 点击「自动检测」按钮，后端分析整段视频，每个场景取代表帧，截图列表追加结果

#### 3.1.4 截图列表管理

| 需求项 | 描述 |
|--------|------|
| 展示 | 缩略图 + 时间戳，列表滚动展示 |
| 选择 | 点击截图高亮选中（用于查看/单删） |
| 单张删除 | 每张截图右上角有 ✕ 按钮，点击删除 |
| 导出 ZIP | 支持 JPG / PNG 格式选择，下载全部截图的压缩包 |
| 状态反馈 | 操作后底部状态栏实时更新文案 |

#### 3.1.5 接口清单

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/upload-video` | 上传视频，返回 `video_id` |
| POST | `/api/detect-scenes` | 触发场景检测，返回截图列表 |
| POST | `/api/capture-frame` | 手动截取指定时间戳帧 |
| GET | `/api/video-file/{video_id}` | 获取视频文件流（供播放器使用） |
| GET | `/api/screenshot-file/{video_id}/{filename}` | 获取单张截图文件 |
| GET | `/api/screenshots/{video_id}` | 获取当前会话截图列表 |
| DELETE | `/api/screenshot/{video_id}/{shot_id}` | 删除单张截图 |
| GET | `/api/export-screenshots/{video_id}` | 导出所有截图为 ZIP |

---

### 3.2 图片拼接

#### 3.2.1 图片上传

| 需求项 | 描述 |
|--------|------|
| 支持方式 | 点击多选 或 拖拽多张图片 |
| 支持格式 | 常见图片格式（JPG、PNG、WebP 等） |
| 行为 | 上传图片追加到当前会话；无会话时新建会话 |
| 来源 | 直接上传 或 从视频截图通过「执行」按钮发送 |

#### 3.2.2 图片列表管理

| 需求项 | 描述 |
|--------|------|
| 展示 | 缩略图网格，左上角显示排序序号 |
| 拖拽排序 | 鼠标拖拽调整图片顺序，松手即生效 |
| 单张删除 | 每张图片右上角 ✕ 按钮 |
| 一键清空 | 列表有图片时，标题栏显示「一键清空」按钮，点击需二次确认后清空所有图片 |
| 图片计数 | 标题栏实时显示当前图片数量 |

#### 3.2.3 拼接参数配置

| 参数 | 说明 | 取值范围 / 选项 |
|------|------|----------------|
| 拼接方向 | 横向（按列排列）/ 纵向（按行排列） | `horizontal` / `vertical` |
| 列数（横向模式） | 每行图片数，行数自动计算 | 1 – 20，整数 |
| 行数（纵向模式） | 每列图片数，列数自动计算 | 整数 |
| 行间距 | 图片行之间的空白像素 | 0 – 200 px |
| 列间距 | 图片列之间的空白像素 | 0 – 200 px |
| 背景色 | 间距区域填充颜色 | 颜色选择器（Hex） |
| 导出格式 | 输出文件格式 | JPEG / PNG |
| JPEG 质量 | 仅 JPEG 模式可用 | 1 – 100，默认 90 |

**推荐布局提示**：根据当前图片的平均宽高比，自动计算最接近 16:9 输出比的推荐列数，显示在参数面板中供参考。

#### 3.2.4 预览与导出

| 需求项 | 描述 |
|--------|------|
| 生成预览 | 点击「生成预览」，后端按当前参数生成缩小版预览图（最大宽度 1200px），展示在右侧预览区 |
| 导出 | 点击「导出」，按当前参数生成全分辨率图片，浏览器触发下载 |
| 图片尺寸逻辑 | 横向模式：所有图等高后横拼；纵向模式：所有图等宽后竖拼 |

#### 3.2.5 接口清单

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/upload-images` | 上传图片，新建拼接会话 |
| POST | `/api/receive-screenshots` | 从视频截图接收图片，新建拼接会话 |
| POST | `/api/reorder-images` | 更新图片排列顺序 |
| DELETE | `/api/stitch-image/{stitch_id}/{img_id}` | 删除单张图片 |
| POST | `/api/stitch-clear/{stitch_id}` | 清空当前会话所有图片 |
| POST | `/api/stitch-preview` | 生成预览图 |
| POST | `/api/stitch-export` | 导出完整拼接图 |
| GET | `/api/stitch-image-file/{stitch_id}/{img_id}` | 获取单张图片文件 |
| GET | `/api/stitch-file/{stitch_id}/{filename}` | 获取拼接结果文件 |

---

### 3.3 AI 调研报告生成

#### 3.3.1 图片上传

| 需求项 | 描述 |
|--------|------|
| 支持方式 | 点击多选 或 拖拽多张图片 |
| 来源 | 直接上传 或 从视频截图通过「执行」按钮发送 |
| 会话重置 | 每次从视频截图「执行」时，强制创建新会话（旧图片不保留） |
| 直接上传 | 追加到当前会话（不重置） |

#### 3.3.2 图片列表管理

与图片拼接模块一致：缩略图展示、拖拽排序、单张删除、一键清空、数量计数。

#### 3.3.3 分析模板选择

提供 5 种预设场景模板，选中后自动填充对应 Prompt：

| 模板名 | 分析重点 |
|--------|----------|
| 产品整体分析 | 产品定位、UI 风格、核心功能、交互亮点、改进建议 |
| UI/UX 设计评估 | 视觉设计、交互设计、信息架构、优缺点 |
| 功能清单梳理 | 功能列表、功能说明、差异化特点、完整性评估 |
| 竞品横向对比 | 功能对比矩阵、设计风格、优劣势对比 |
| 完全自定义 | 用户自由编辑 Prompt |

- 切换模板时，Prompt 文本框内容自动替换
- 选中「完全自定义」后，文本框可自由编辑

#### 3.3.4 报告生成

| 需求项 | 描述 |
|--------|------|
| 触发 | 点击「生成报告」按钮 |
| 图片限制 | 最多 20 张图片同时发送给 AI |
| 图片格式 | 转为 Base64 编码后随 Prompt 一并传给 Claude API |
| AI 模型 | claude-sonnet-4-6，max_tokens 4096 |
| 输出格式 | Markdown，渲染展示在页面报告区 |
| 生成中状态 | 按钮禁用 + 状态栏显示「正在调用 Claude API 生成报告...」 |

#### 3.3.5 报告输出操作

| 操作 | 描述 |
|------|------|
| 复制 | 复制 Markdown 原文到剪贴板，状态栏提示「已复制到剪贴板」 |
| 下载 | 下载为 `.md` 文件 |
| 重新生成 | 修改 Prompt 或图片后可再次点击「生成报告」覆盖上次结果 |

#### 3.3.6 接口清单

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/receive-report-screenshots` | 从视频截图接收图片，创建新报告会话 |
| POST | `/api/upload-report-images` | 直接上传图片，追加到当前会话 |
| DELETE | `/api/report-image/{report_id}/{img_id}` | 删除单张图片 |
| POST | `/api/report-clear/{report_id}` | 清空当前会话所有图片 |
| GET | `/api/report-image-file/{report_id}/{img_id}` | 获取单张图片文件 |
| POST | `/api/generate-report` | 调用 Claude API 生成报告 |

---

### 3.4 跨模块工作流（执行按钮）

视频截图列表下方有一个统一操作区，支持将截图一键分发到其他模块。

#### 3.4.1 操作选项

| 选项 | 描述 |
|------|------|
| 图片拼接（复选框） | 将当前截图列表发送到「图片拼接」模块 |
| 生成报告（复选框） | 将当前截图列表发送到「AI 调研报告」模块 |
| 执行按钮 | 按勾选情况执行，至少需勾选一项 |

#### 3.4.2 执行逻辑

| 勾选情况 | 行为 |
|----------|------|
| 仅勾选「图片拼接」 | 发送截图到拼接，完成后自动跳转到「图片拼接」标签 |
| 仅勾选「生成报告」 | 重置报告会话，发送截图到报告，完成后跳转到「调研报告」标签 |
| 同时勾选两项 | 按顺序执行两者，最终跳转到「调研报告」标签；报告模块状态栏额外显示「图片拼接也已完成，可切换至拼接标签查看」 |
| 均未勾选 | 弹出提示：「请至少选择一个操作」，不执行 |
| 没有截图 | 弹出提示：「没有截图可发送」，不执行 |

#### 3.4.3 会话重置规则

- **图片拼接**：每次执行均新建拼接会话（旧内容清空）
- **调研报告**：每次执行均新建报告会话（旧图片清空）
- 目的：避免多次执行导致旧图片与新截图混合

---

## 4. 非功能性需求

### 4.1 性能

| 指标 | 要求 |
|------|------|
| 视频上传 | 支持常见录屏文件大小（建议 ≤ 500MB），上传期间不阻塞页面 |
| 场景检测 | 5 分钟视频检测时间 ≤ 30s（依赖服务器性能） |
| 图片拼接预览 | 生成预览 ≤ 5s（20 张以内常规图片） |
| 报告生成 | 依赖 Claude API 响应，通常 10–30s，页面需有明确等待状态 |

### 4.2 并发

- 当前为无账号体系的多用户场景
- 不同用户通过独立 `session_id`（`video_id` / `stitch_id` / `report_id`）隔离数据
- 无需用户登录，`session_id` 由前端持有（页面刷新后丢失）
- 服务端数据存储于内存 + 临时文件系统，重启后清空

### 4.3 错误处理

| 场景 | 处理方式 |
|------|----------|
| 上传失败 | 状态栏显示错误信息 |
| 场景检测失败 | 状态栏显示错误，不影响已有截图 |
| Claude API 无密钥 | 生成报告时返回明确错误提示 |
| Claude API 调用失败 | 状态栏显示错误原因，按钮恢复可用 |
| 图片文件不存在 | 接口返回 404，前端提示用户 |
| 清空/删除失败 | 状态栏显示错误原因 |

### 4.4 浏览器兼容性

- 目标：Chrome / Edge 最新版
- 依赖特性：HTML5 `<video>`、File API、Drag & Drop API、Clipboard API、Fetch API

### 4.5 响应式

- 主要面向桌面端（宽屏）
- 视口宽度 < 900px 时，布局自动折叠为单列

---

## 5. 技术约束与部署说明

### 5.1 环境依赖

| 依赖 | 说明 |
|------|------|
| Python ≥ 3.10 | 后端运行环境 |
| `ANTHROPIC_API_KEY` | 环境变量，必须配置，报告生成功能依赖 |
| OpenCV | 视频帧提取 |
| PySceneDetect | 场景检测算法 |
| Pillow | 图片处理与拼接 |
| FastAPI + Uvicorn | Web 服务框架 |

### 5.2 目录结构

```
video-image-tool/
├── main.py                   # FastAPI 入口
├── routers/
│   ├── screenshot.py         # 视频截图相关接口
│   ├── stitch.py             # 图片拼接相关接口
│   └── report.py             # 报告生成相关接口
├── services/
│   ├── scene_detect.py       # 场景检测 & 截帧逻辑
│   ├── image_stitch.py       # 图片拼接逻辑
│   └── report_generator.py   # Claude API 调用逻辑
├── static/
│   ├── index.html            # 主页面
│   ├── app.js                # 前端交互逻辑
│   └── style.css             # 样式
└── temp/                     # 运行时临时文件目录
    ├── {video_id}/           # 视频 & 截图文件
    ├── stitch/{stitch_id}/   # 拼接会话图片 & 输出
    └── report/{report_id}/   # 报告会话图片
```

### 5.3 启动方式

```bash
pip install -r requirements.txt
export ANTHROPIC_API_KEY=sk-ant-xxx
uvicorn main:app --reload --port 8000
```

### 5.4 数据生命周期

- 所有会话数据存储于内存，服务重启后清空
- 临时文件存于 `temp/` 目录，不会自动清理（需运维定期清理或实现自动 GC）

---

## 6. 未来规划

以下为当前版本未覆盖、建议后续迭代的功能方向：

### P1 优先级（核心体验提升）

| 需求 | 描述 |
|------|------|
| 临时文件自动清理 | 设定文件过期时间（如 24h），定时清理 `temp/` 避免磁盘堆积 |
| 报告模板自定义 | 用户可保存自定义 Prompt 模板，下次使用时可选 |
| 截图批量选择与删除 | 支持多选截图，批量删除 |
| 拼接会话复用 | 支持追加图片到已有拼接会话（当前每次执行均重置） |
| 视频时间范围过滤 | 场景检测时支持指定起止时间区间，只分析视频片段 |

### P2 优先级（功能完善）

| 需求 | 描述 |
|------|------|
| 报告流式输出 | Claude 生成报告时支持 streaming，减少用户等待感 |
| 截图标注 | 在截图上添加文字标注或框选，再发送到拼接/报告 |
| 多视频管理 | 支持同时上传多个视频，在会话列表中切换 |
| 拼接结果历史 | 保留最近 N 次拼接结果，可回溯查看 |
| 图片压缩选项 | 上传图片时支持自动压缩，优化大图传输速度 |

### P3 优先级（长期规划）

| 需求 | 描述 |
|------|------|
| 数据持久化 | 引入数据库，会话数据在服务重启后仍可恢复 |
| 导出报告为 PDF / DOCX | 报告支持更多导出格式 |
| 批量视频处理 | 上传多个视频批量截图，合并到同一分析会话 |
| 报告版本管理 | 同一批图片支持保存多次生成的报告，对比不同 Prompt 的输出 |

---

## 7. 验收标准

### 7.1 视频截图提取

- [ ] 上传视频后，播放器正常加载并可播放
- [ ] 手动点击截帧，截图准确对应当前播放时间戳
- [ ] 三种场景检测算法均可正常触发并返回截图
- [ ] 切换算法后，阈值滑块范围与默认值自动更新
- [ ] 单张截图删除后，列表实时更新
- [ ] 导出 ZIP 包含所有当前截图，格式与选择一致

### 7.2 图片拼接

- [ ] 直接上传图片后，缩略图正常展示，序号正确
- [ ] 拖拽排序后，预览与导出的图片顺序与列表一致
- [ ] 单张删除、一键清空（需二次确认）均正常生效
- [ ] 横向 / 纵向模式切换，参数面板对应显示列数 / 行数输入
- [ ] 生成预览图与参数配置一致（方向、列数、间距、背景色）
- [ ] 导出图片为全分辨率，格式与选择一致
- [ ] 推荐布局提示随图片数量变化实时更新

### 7.3 AI 调研报告生成

- [ ] 5 种模板选择后，Prompt 文本框自动填充对应内容
- [ ] 「完全自定义」模式下，Prompt 可自由编辑
- [ ] 点击生成报告后，按钮禁用，状态栏显示等待中
- [ ] 报告生成完成后，Markdown 正确渲染
- [ ] 复制按钮将 Markdown 原文复制到剪贴板
- [ ] 下载按钮生成 `.md` 文件并触发浏览器下载
- [ ] 未配置 API Key 时，生成报告返回明确错误提示

### 7.4 跨模块工作流

- [ ] 无截图时点击「执行」，弹出提示
- [ ] 未勾选任何选项时点击「执行」，弹出提示
- [ ] 仅勾选「图片拼接」执行后，自动跳转拼接标签，图片列表正确
- [ ] 仅勾选「生成报告」执行后，自动跳转报告标签，每次执行图片列表重置（不保留旧图）
- [ ] 同时勾选两项执行后，跳转报告标签，状态栏包含拼接完成提示
- [ ] 同时勾选两项时，拼接标签中图片列表也正确更新

### 7.5 通用交互

- [ ] 所有耗时操作在状态栏有明确的进行中/完成/失败反馈
- [ ] 页面三个 Tab 切换正常，各模块状态独立保持
- [ ] 图片列表有图时「一键清空」按钮显示，无图时隐藏
